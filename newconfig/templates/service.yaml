apiVersion: v1
kind: Service
metadata:
  name: {{ include "newconfig.fullname" . }}
  labels:
    {{- include "newconfig.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "newconfig.selectorLabels" . | nindent 4 }}
